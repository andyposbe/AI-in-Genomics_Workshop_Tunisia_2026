# Scripts

| Script | What it does |
|---|---|
| `check_setup.py` | Participant-facing pre-workshop check. Standard library only, no install needed. |

<!-- FILL IN: add the helper scripts for Practicals 3 and 4 as the toolchains are
     settled: the distance-matrix plot for P3 and the design filter for P4.
     Keep them dependency-light and give each one a --help. -->

## Conventions

- Runnable from the repository root: `python3 scripts/<name>.py`
- Print what they are doing; never fail silently
- No hard-coded paths outside the repository
